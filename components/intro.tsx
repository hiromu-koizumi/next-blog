import { CMS_NAME } from '../lib/constants'

const Intro = () => {
  return (
    <section className="flex-col md:flex-row flex items-center md:justify-between mt-3 mb-3 md:mb-12">
      <h1 className="text-2xl md:text-5xl font-bold leading-tight md:pr-8">
        CryptoClips
      </h1>
    </section>
  )
}

export default Intro
